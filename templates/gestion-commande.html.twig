{% extends 'base.html.twig' %}

{% block header %}
<header>
    <div class="container">
        <h1>Gestion des commandes de réapprovisionnement</h1>
        <p>Gérez vos commandes ici</p>
    </div>
    <div class="login-button-container">
        <a href="index.php?uri=/" class="login-button">Revenir à l'accueil</a>
    </div>
</header>
{% endblock %}

{% block content %}
<main>
    <section class="search-section">
        <form action="index.php" method="GET">
            <input type="hidden" name="uri" value="order-management">
            <label for="search">Rechercher une commande :</label>
            <input type="text" id="search" name="search" placeholder="Recherche par produit, fournisseur...">
            <button type="submit" class="searchBtn">
                🔍 Rechercher
            </button>
        </form>

        <div id="filterBar" class="filterBar">
            <select id="statusFilter" class="statusFilter" onchange="filterOrders()">
                <option value="all">Tous les statuts</option>
                <option value="en attente">En attente</option>
                <option value="validée">Validée</option>
                <option value="livrée">Livrée</option>
                <option value="annulée">Annulée</option>
            </select>

            <select id="categoryFilter" class="categoryFilter" onchange="filterOrders()">
                <option value="all">Toutes les catégories</option>
                {% if categories is defined %}
                    {% for category in categories %}
                        <option value="{{ category.id }}">{{ category.nom }}</option>
                    {% endfor %}
                {% endif %}
            </select>
        </div>
    </section>

    <section class="product-section">
        <div class="container">
            {% if orders is defined and orders is not empty %}
                <table>
                    <thead>
                        <tr>
                            <th>N° Commande</th>
                            <th>Produit</th>
                            <th>Catégorie</th>
                            <th>Prix</th>
                            <th>Quantité</th>
                            <th>Fournisseur</th>
                            <th>Date livraison</th>
                            <th>Statut</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for order in orders %}
                            <tr>
                                <td>CMD-{{ order.id }}</td>
                                <td>{{ order.product_name }}</td>
                                <td>{{ order.category_name }}</td>
                                <td>{{ order.prix }} €</td>
                                <td>{{ order.quantite }}</td>
                                <td>{{ order.supplier_name }}</td>
                                <td>{{ order.date_livraison|date('d/m/Y') }}</td>
                                <td>
                                    {% set statusClass = order.statut|replace({'_': '-', ' ': '-'}) %}
                                    <span class="status-badge status-{{ statusClass }}">
                                        {% set status_labels = {
                                            'en attente': 'En attente',
                                            'en_attente': 'En attente',
                                            'en_preparation': 'En préparation',
                                            'expediee': 'Expédiée',
                                            'en_transit': 'En transit',
                                            'livrée': 'Livrée',
                                            'annulée': 'Annulée'
                                        } %}
                                        {{ status_labels[order.statut] ?? order.statut }}
                                        <br>
                                        <small>
                                            {% if order.statut == 'en_attente' %}
                                                Préparation prévue : {{ order.date_preparation_prevue|date('d/m/Y') }}
                                            {% elseif order.statut == 'en_preparation' %}
                                                Expédition prévue : {{ order.date_expedition_prevue|date('d/m/Y') }}
                                            {% elseif order.statut == 'expediee' %}
                                                Transit prévu : {{ order.date_transit_prevue|date('d/m/Y') }}
                                            {% elseif order.statut == 'en_transit' %}
                                                Livraison prévue : {{ order.date_livraison|date('d/m/Y') }}
                                            {% endif %}
                                        </small>
                                    </span>
                                </td>
                                <td>
                                    {% if order.statut in ['en attente', 'en_attente', 'en_preparation'] %}
                                        <a href="index.php?uri=modify-order&id={{ order.id }}" class="btn-edit" title="Modifier">✏️</a>
                                        <a href="index.php?uri=delete-order&id={{ order.id }}" 
                                           onclick="return confirm('Êtes-vous sûr de vouloir supprimer cette commande ?')"
                                           class="btn-delete" title="Supprimer">🗑️</a>
                                    {% endif %}
                                    <a href="index.php?uri=tracking-order&id={{ order.id }}" class="btn-track" title="Suivi">📦</a>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            {% else %}
                <p class="no-results">Aucune commande trouvée</p>
            {% endif %}
        </div>
    </section>

    <div class="ajouter">
        <a href="index.php?uri=add-order" class="login-button">Nouvelle commande +</a>
    </div>
</main>
{% endblock %}